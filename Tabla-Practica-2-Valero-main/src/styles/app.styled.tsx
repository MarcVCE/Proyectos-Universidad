import styled from "styled-components";

export const TablaPersona = styled.div`
  display: grid;
  grid-template-columns: repeat(4,300px);
`

export const TablaHead = styled.div`
  font-weight: bold;
  font-size: large;
`